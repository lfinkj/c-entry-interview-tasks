#include <stdlib.h>

// system(clear) places in separate file, easy to change for adapting to other consoles if needed
void clearTerminal( void ) {

    system( "clear" ); // clear terminal screen
} 
